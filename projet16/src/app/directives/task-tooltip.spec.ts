import { TaskTooltip } from './task-tooltip';

describe('TaskTooltip', () => {
  it('should create an instance', () => {
    const directive = new TaskTooltip();
    expect(directive).toBeTruthy();
  });
});
