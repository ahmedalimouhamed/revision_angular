import { PriorityHighlight } from './priority-highlight';

describe('PriorityHighlight', () => {
  it('should create an instance', () => {
    const directive = new PriorityHighlight();
    expect(directive).toBeTruthy();
  });
});
