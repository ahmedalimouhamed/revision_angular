import { TemperatureColor } from './temperature-color';

describe('TemperatureColor', () => {
  it('should create an instance', () => {
    const directive = new TemperatureColor();
    expect(directive).toBeTruthy();
  });
});
