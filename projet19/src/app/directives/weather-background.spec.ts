import { WeatherBackground } from './weather-background';

describe('WeatherBackground', () => {
  it('should create an instance', () => {
    const directive = new WeatherBackground();
    expect(directive).toBeTruthy();
  });
});
