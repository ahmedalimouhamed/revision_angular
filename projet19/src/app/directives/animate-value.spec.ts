import { AnimateValue } from './animate-value';

describe('AnimateValue', () => {
  it('should create an instance', () => {
    const directive = new AnimateValue();
    expect(directive).toBeTruthy();
  });
});
