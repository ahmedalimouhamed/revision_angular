import { PulseAnimation } from './pulse-animation';

describe('PulseAnimation', () => {
  it('should create an instance', () => {
    const directive = new PulseAnimation();
    expect(directive).toBeTruthy();
  });
});
