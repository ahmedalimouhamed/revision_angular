import { ColorByRating } from './color-by-rating';

describe('ColorByRating', () => {
  it('should create an instance', () => {
    const directive = new ColorByRating();
    expect(directive).toBeTruthy();
  });
});
