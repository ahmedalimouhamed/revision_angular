import { HighlightMention } from './highlight-mention';

describe('HighlightMention', () => {
  it('should create an instance', () => {
    const directive = new HighlightMention();
    expect(directive).toBeTruthy();
  });
});
