import { AutoExpand } from './auto-expand';

describe('AutoExpand', () => {
  it('should create an instance', () => {
    const directive = new AutoExpand();
    expect(directive).toBeTruthy();
  });
});
