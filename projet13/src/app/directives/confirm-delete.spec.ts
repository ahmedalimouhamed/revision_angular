import { ConfirmDelete } from './confirm-delete';

describe('ConfirmDelete', () => {
  it('should create an instance', () => {
    const directive = new ConfirmDelete();
    expect(directive).toBeTruthy();
  });
});
