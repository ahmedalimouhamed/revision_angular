import { AppAutoFocus } from './app-auto-focus';

describe('AppAutoFocus', () => {
  it('should create an instance', () => {
    const directive = new AppAutoFocus();
    expect(directive).toBeTruthy();
  });
});
